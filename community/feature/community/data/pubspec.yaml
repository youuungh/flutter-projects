name: community_data
description: "feature community data package."
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  community_domain:
    path: ../domain

  tool_community_network:
    path: ../../../tool/community/network