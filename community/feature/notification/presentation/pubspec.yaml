name: notification_presentation
description: "feature notification community presentation package."
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  notification_domain:
    path: ../domain

  core_bloc:
    path: ../../../core/bloc
  core_util:
    path: ../../../core/util